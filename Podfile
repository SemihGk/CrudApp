# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'DiceUp!' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
    pod 'Dollar', '~> 5.0.0'
    pod 'Cent', '~> 5.0.0'
    pod 'AsyncSwift'
  use_frameworks!

  # Pods for DiceUp!

  target 'DiceUp!Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DiceUp!UITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
